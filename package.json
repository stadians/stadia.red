{
  "dependencies": {
    "webextension-polyfill": "v0.6"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "v70.0.1",
    "@types/jest": "^25.2.1",
    "import-sort-cli": "^6.0.0",
    "jest": "v26.0.1",
    "prettier": "v2.0.5",
    "replace-in-files-cli": "v0.3.1",
    "sort-json": "v2",
    "typescript": "v3.8.3"
  },
  "engines": {
    "node": "v14.2.0"
  },
  "jest": {
    "testEnvironment": "jest-environment-node"
  },
  "name": "stadia.observer",
  "private": true,
  "scripts": {
    "build": "./build",
    "test": "yarn build && yarn jest docs/ --testMatch='**/?(*.)+(spec|test).[j]s?(x)'",
    "w": "yarn build; printf '\nwatching to watch\n'; bash -c 'sleep 12'; yarn tsc --watch"
  },
  "type": "module",
  "version": "2020.5.8"
}
