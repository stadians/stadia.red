{
  "devDependencies": {
    "prettier": "^2.0.5",
    "sort-json": "^2.0.0",
    "typescript": "^3.8.3"
  },
  "engines": {
    "node": "^14.0.0"
  },
  "name": "stadia.observer",
  "private": true,
  "scripts": {
    "build": "rm -rf docs/* && cp -r src/* data/* images/* docs/ && sort-json *.json */*.json; prettier --write src *.json */*.json && (tsc || echo \"type errors oh no\") && prettier --write docs"
  },
  "version": "0.18.18"
}
