{
  "name": "stadia.obserer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": " prettier --write src && rm -rf dist/* && tsc && prettier --write dist"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "sort-json": "^2.0.0",
    "typescript": "^3.8.3"
  }
}
