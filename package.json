{
  "name": "stadia.obserer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/* && tsc && tsc --outFile dist/all.js --target es2015 --module amd && prettier --write src dist"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "typescript": "^3.8.3"
  }
}
